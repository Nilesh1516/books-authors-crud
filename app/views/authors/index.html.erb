<h1>Authors!</h1>
<%= link_to "New Author", new_author_path %>
<div>
  <ul>
    <% @authors.each do |author| %>
    <li><%= link_to author.first_name, author_path(author) %> (Last_name. <%= author.last_name %>) (b. <%= author.birth_year %>)</li>
    <% end %>
  </ul>
</div>
